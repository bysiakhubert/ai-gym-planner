---
import AuthLayout from "@/layouts/AuthLayout.astro";
import { ForgotPasswordForm } from "@/components/auth";

export const prerender = false;

// Note: This page is accessible to both authenticated and unauthenticated users
// Users may want to reset their password even if they're logged in
---

<AuthLayout title="Resetowanie hasła" description="Zresetuj hasło do swojego konta GymPlanner">
  <ForgotPasswordForm client:load />
</AuthLayout>
