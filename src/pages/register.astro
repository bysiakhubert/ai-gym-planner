---
import AuthLayout from "@/layouts/AuthLayout.astro";
import { RegisterForm } from "@/components/auth";

export const prerender = false;

// Note: Server-side session check and redirect to dashboard
// is handled automatically by the authentication middleware.
// See: src/middleware/index.ts
---

<AuthLayout title="Rejestracja" description="Utwórz nowe konto w GymPlanner">
  <RegisterForm client:load />
</AuthLayout>

